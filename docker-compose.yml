version: "3.8"
services:
  pg-db:
    image: postgres:16
    environment:
      - POSTGRES_PASSWORD=${DATABASE_PASSWORD}
    volumes:
      - db-data:/var/lib/postgresql/data
    ports:
      - 5432:5432

  redis-db:
    image: redis:7.2.3
    ports:
      - 6379:6379
volumes:
  db-data:
